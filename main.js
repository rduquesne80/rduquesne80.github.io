const topAppBarElement = document.querySelector('.mdc-top-app-bar');
const topAppBar = new MDC.MDCTopAppBar(topAppBarElement);

const tabBar = new MDC.MDCTabBar(document.querySelector('.mdc-tab-bar'));